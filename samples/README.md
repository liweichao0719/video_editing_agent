# 本地样例素材

此目录只保存本地视频，不提交视频文件到 Git。

演示和集成测试使用 Wikimedia Commons 的 Kitchen blender（CC BY 3.0）：

```bash
curl -L 'https://upload.wikimedia.org/wikipedia/commons/5/5f/Kitchen_blender.webm' \
  -o samples/test_blender_av.webm
```

来源与署名信息见 `evaluation/data/metadata.json`。
